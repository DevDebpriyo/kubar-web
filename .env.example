# SMTP connection used by the server-side contact route.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-user
SMTP_PASSWORD=your-smtp-password

# Destination for contact-form messages.
CONTACT_FORM_RECIPIENT_EMAIL=outreach@example.com
